function update(elapsed) {
    myStateManager.update(elapsed);
}
function draw() {
    myStateManager.draw();
}
function mousePressed(x, y) {
    myStateManager.mousePressed(x, y);
}
function keyPressed(key) {
    myStateManager.keyPressed(key);
}
function keyReleased(key) {
    myStateManager.keyReleased(key);
}
